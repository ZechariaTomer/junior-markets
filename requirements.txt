# --- Core ---
Django==4.2.24
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
python-dotenv==1.0.1

# --- Database drivers (choose by env) ---
# SQLite מובנה, לא צריך חבילה
psycopg2-binary==2.9.9    # נחוץ רק כשמשתמשים ב-PostgreSQL

# --- Cache / Redis (אופציונלי; יעבוד אוטומטית אם REDIS_URL קיים) ---
django-redis==5.4.0
redis==5.0.8
